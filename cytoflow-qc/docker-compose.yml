version: "3.9"

services:
  cytoflow-qc:
    build: .
    image: cytoflow-qc:latest
    command: ["run", "--samplesheet", "samplesheets/example_samplesheet.csv", "--config", "configs/example_config.yaml", "--out", "results"]
    volumes:
      - ./:/workspace
    working_dir: /workspace
